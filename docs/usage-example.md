# Usage Example

The most common "usage" of this repository is contributing a documentation update:

```bash
git checkout -b docs/my-update
# edit a file under docs/
git add docs/my-update.md
git commit -m "docs: describe my update"
git push origin docs/my-update
```

Then open a pull request describing the change and, if it addresses one, link the related issue in the description.

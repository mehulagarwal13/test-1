# Introduction

`test-1` is a small example/test repository used to exercise real GitHub workflows: branching, committing, opening pull requests, and tracking work through issues.

It has no application code today -- its purpose is to be a safe, low-stakes place to practice and verify a real contribution workflow end to end, from a tracked issue through a reviewed (but not merged) pull request.

# Testing

```bash
python tests/validate_repo.py
```

This confirms the repository's core documentation files exist and are non-empty. If you add a new required file, add it to `REQUIRED_FILES` in `tests/validate_repo.py` rather than writing a parallel check.

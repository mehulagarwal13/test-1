# Installation

```bash
git clone https://github.com/mehulagarwal13/test-1.git
cd test-1
```

There is no dependency installation step: the repository has no external dependencies. `tests/validate_repo.py` uses only the Python standard library.
